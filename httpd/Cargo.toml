[package]
name = "odin_httpd"
version = "0.1.0"
edition = "2021"

[dependencies]
odin_core = { path = "../core" }
odin_subsystem = { path = "../subsystem" }

axum = "0.7"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
